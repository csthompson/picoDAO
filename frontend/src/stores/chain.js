import { writable } from "svelte/store";

export const governorABI = writable({});
export const governorContract = writable({});

export const tokenABI = writable({});
export const tokenContract = writable({});